This is a test file.
"git init" is to initialize git on the folder
"git status" is to see the status of files being tracked.
"git add file.###" is to add file to tracking by git.
"git commit -m "message"" is to add the edits to the git tracking
"git push origin master" is to upload to the github repository
"git pull origin master" is to download from the github repository.
"git log" shows the changes made to files and the repository


"git config --global user.name "Judah Nava"" is to set up username
"git config --global user.email judah.nava@go.mnstate.edu"
"git config --list"
